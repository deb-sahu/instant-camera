import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Instant Camera Test', () {
    // Test code goes here.
    runApp(MaterialApp(home: Scaffold(body: Container())));
  });
}

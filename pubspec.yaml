name: instant_camera
description: A flutter plugin to quickly take pictures and videos using a built-in camera interface.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.3.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9 
  path_provider: ^2.0.14
  permission_handler: ^10.2.0
  gallery_saver: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:

